// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

export const firebaseConfig = {
  apiKey: "AIzaSyCmAl5flJO9X5sBxFt-47ZrJSDj1N8kH2Y",
  authDomain: "chatonline-a60c7.firebaseapp.com",
  databaseURL: "https://chatonline-a60c7.firebaseio.com",
  projectId: "chatonline-a60c7",
  storageBucket: "chatonline-a60c7.appspot.com",
  messagingSenderId: "325442253002",
  appId: "1:325442253002:web:49868db33e30665d"
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
